# PR20: huecos de auditoría que sí pueden cambiar el merge

20-sep-2026 ART. Pedido: Find audit gaps that could change the merge decision.

## Decisión recomendada

**Pausar el merge por una carencia concreta de protección contra regresiones, no por un defecto nuevo del producto.** El banco Chromium versionado aprueba una mutación que hace abrir y guardar el primer resultado al pulsar cualquier resultado. El head real pasa el nuevo oráculo; no afirmo que hoy elija mal. Incorporar esa prueba a CI y obtener revisión separada del fix de logout serían mis condiciones de aprobación. Son recomendaciones de auditoría, no reglas de protección de rama verificadas ni permiso para modificar o fusionar.

Sujeto: PR20 `cc2ca30760c486cad3154c619a20f94cf75cc691`, base PR19 `31dff1ed1c066d4af8a3c6a1ac45e0e85d21eb05`. Main consultado `73087d837f2bbb173d8a6608e2555c781d02e211`. PR20 abierto y sin merge en consulta inicial/final. Productor inicial BRAIN; fix logout Astra. La revisión de esta instancia no se presenta como revisión externa personalmente independiente.

## G1 CONFIRMADO: el banco no vincula el clic no inicial con lo leído y descargado

En `tests/browser_discovery.cjs`, la lectura/descarga positiva siempre selecciona `.first()`. Ben se comprueba mediante texto de resultados, no mediante una descarga de su segundo documento. `download_pins_selected_version` compara el nombre del archivo con UID/version dentro del propio archivo; eso acredita consistencia interna, no que corresponda al resultado elegido.

Falsador local en el callback de selección de `sistema/web/discovery_spike.html`: reemplazar `uid = item.uid; version = item.version;` por `uid = r.results[0].uid; version = r.results[0].version;`. No cambiar tests del autor ni backend. Cada botón queda ligado erróneamente al primero, pero los permisos siguen funcionando. No es una filtración entre colecciones.

Medición real Chromium153.0.8010.12: banco del autor sobre original **59/59, exit0**; mismo banco sobre mutante **59/59, exit0**. No se deduce detección desde el retorno: precisamente aquí el retorno verde oculta la violación.

Nuevo instrumento `selection.cjs`, oráculo literal separado del producto: seleccionar/leer/descargar a1, luego cambiar a a2 dentro de la misma sesión y, después de logout/login, seleccionar/leer/descargar b2. Comprueba limpieza de texto y referencia anterior, versión elegida, texto exacto, UID/hash/nombre/URL/cantidad de caracteres de la descarga contra el documento pulsado. Usa CLI/HTTP/navegador reales; no deriva el esperado de CATALOG, del fichero descargado ni del callback.

Original **31/0, exit0**; repetición en nueva fixture **31/0, exit0**. Mutante **31 aserciones, 10 fallos dirigidos, exit1**: para a2 y b2 fallan versión, texto, locator, filename y procedencia. Las otras21 comprobaciones pasan. Fuentes, diff y valores completos conservados.

Impacto sobre merge: el recorrido central buscar -> elegir -> leer -> guardar puede romperse sin que este banco CI lo advierta. La auditoría07 HTTP/paginación no lo cerraba: no atravesaba el callback del navegador ni la descarga. Esta corrida reduce la incertidumbre sobre el head actual, pero el banco nuevo está publicado como evidencia, **NO instalado en CI**.

Cierre exigible: añadir al banco versionado los dos cambios de selección, comparar con golden independiente y demostrar que la misma prueba pasa en el head real y falla con este mutante por locator/texto equivocado. Una prueba basada solo en filename==contenido no basta. Cambiar tests/producto requiere una orden aparte; no lo hice.

## G2 CONFIRMADO en el alcance consultado: falta revisión externa registrada del arreglo

La API `/pulls/20/reviews?per_page=100` devuelve `[]`. Las entregas previas03-05 atribuyen fix y verificación al mismo autor; la descripción vigente declara revisión externa pendiente. No pruebo ausencia de conversaciones privadas ni afirmo que GitHub exija una aprobación configurada.

Impacto: no contar CI, otra etiqueta de modelo o estos informes como segunda revisión de selección de casos. Recomiendo un revisor distinto que revise específicamente token/logoutToken, limpieza inmediata, reintento del bearer original y criterio `logged_out === true`; puede aprovechar las evidencias existentes. No hace falta repetir todo Corpus. Este operador no puede cerrar esa separación afirmándose independiente.

## Integración: no inventar un tercer bloqueo

PR20 apunta a PR19, no a main; PR19 apunta a PR18 y ambos siguen abiertos. Fusionar PR20 en su base no publica toda la cadena en main. Respecto de main hay15archivos de producto/tests/workflow distintos, frente a los10archivos totales del incremento PR20 sobre PR19; son denominadores distintos.

Se ejecutó `git merge-tree --write-tree MAIN PR20`, sin mover ramas ni crear un commit de merge: exit0, árbol `d804583853fd146a2fc07079b7b3b518aa8935b3`. Diff de ese árbol contra el head probado en `sistema`, `tests`, `.github`: vacío. Main no agregó cambios en esas rutas desde el ancestro común. **REFUTADO para este snapshot** un bloqueo técnico por conflicto o producto combinado distinto. Esta comprobación es equivalencia de árboles, no ejecución del merge ni autorización para saltar revisiones de PR18/19.

Si cambian base/head/main con cambios funcionales, volver a fijar árbol y CI. No exijo otro banco completo solo porque main recibió documentación.

## Límites que no subiría a bloqueo de esta demo

BFCache del documento restaurado sigue NO MEDIDO en el antecedente; no es prueba de fuga. TLS/producción, carga, autorización jurídica, revocación atómica y piloto quedan fuera del contrato sintético local. No utilizarlos como sustituto del hueco G1 comprobado. Tampoco reabrir el viejo reparo del body del PR: ya fue corregido.

No nuevo CI ni banco Python en este turno. Se consultó el check vigente stable_identity success; no se confunde con la nueva corrida local ni con stdout del runner. No se midieron todas las variantes de navegador, respuestas malformadas o concurrencia. El hallazgo es del banco y su poder de detección, no un bug activo ni una certificación integral.

## Evidencia, recursos y cierre

Archivo hermano `2026-09-20-08-merge-gaps/evidence.xz.b64`, base64 estricto+xz, JSON69062bytes SHA256 `cc668501db9a566050a0d272b7c16d46ef502d054c03a8e40685b3eacdb075db`; transporte11228caracteres. Contiene fuentes, diff, comandos, entornos, stdout/stderr y resultados completos de las cinco corridas, metadatos seleccionados de PR, reviews, árbol virtual y comprobaciones de recursos. Token/password/SQLite y metadatos completos de la API excluidos explícitamente. Captura inicial local más extensa95609bytes no se presenta como publicada.

Tres servidores del instrumento nuevo terminaron exit0 y sus puertos devolvieron111 en reconsulta. Escaneo de servidores del scratch por `/proc`: vacío; no se certifican todos los procesos internos del navegador. El banco del autor conserva su propia limpieza y el scan no encontró servidores residuales de sus copias. Error de descubrimiento: intentar listar el cache de root dio PermissionError; se recuperó el HOME y entorno reales del recibo anterior, sin instalar ni cambiar permisos.

Reproducción: extraer cc2ca307 a una copia, decodificar/verificar evidencia, guardar `sources["selection.cjs"]` y ejecutar `node selection.cjs REPO NUEVO_OUTPUT` con Playwright1.63.0 y Chromium disponible. Aplicar `mutation.diff` solo a otra copia y ejecutar tanto `tests/browser_discovery.cjs` como el nuevo instrumento. Cada output debe ser nuevo.

Gate I PASA en alcance de G1: positivo, repetición y falsador contrastados; G2/integración son inspección API/git, no simulación experimental. Gate II PASA: diferencia bug actual, hueco CI, review externa y alcance de merge. Gate III al redactar: pendiente de publicar y recuperar este informe/evidencia, Doc público y Nexus; su cierre posterior registrará esas verificaciones sin ampliar el veredicto.

Sin cambios a ramas de producto, core, tests versionados, PR body, estados, comentarios, issues ni despliegue. Solo instrumentos y mutante en copias propias y entrega documental autorizada.
