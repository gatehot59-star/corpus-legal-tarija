# Auditoría de Brain: PR18, PR19 y diagnóstico del piloto

19-sep-2026 ART. Pedido: «audita lo de brain y da». Se auditan las tres entregas posteriores a Nexus58; la frase final incompleta no se interpreta como permiso de merge, despliegue, mensajes o nuevas funciones. Auditor y verificador experimental son funciones del mismo operador. Implementé el spike original de PR18: no lo presento como trabajo ajeno ni esta revisión como independencia personal total.

## Veredicto acotado

**Sin nuevos reparos en las propiedades probadas de la demo. Brain corrigió un punto ciego real del test Unicode y PR19 rechaza el guardado cuando pierde autorización. Esto no habilita un piloto real ni equivale a una auditoría completa de autenticación.**

Sujetos congelados: main documental `3cd773babcf928f9877d89dc33b024a4a9cc1739`; PR18 `2231ca8edbe4c0b6eba88156d5abd087bbf9e51f`; PR19 `31dff1ed1c066d4af8a3c6a1ac45e0e85d21eb05`, apilado sobre PR18. Ambos abiertos y sin merge en la consulta final previa a publicar. Este commit agrega solamente informe y evidencia a main; no integra ninguno de los PR.

## 1. PR18: test Unicode y CI

CONFIRMADO experimentalmente: en copia del viejo head `b701b8c704ea57791c642a6d534e208cfa071356`, cambiar `Array.from(r.text).length` por `r.text.length` deja pasar el banco anterior, exit0. El mismo mutante sobre `2231ca8` falla específicamente en `astral_codepoint_response_contract`, no por preparación o entorno. El banco nuevo es capaz de detectar esa regresión.

Es un defecto del test anterior, no un defecto del cliente original, que ya contaba puntos de código correctamente. El caso nuevo intercepta una respuesta y conserva el hash anterior: prueba contrato de consumidor, NO integridad criptográfica end-to-end de una fuente astral. Brain lo declara correctamente.

CONFIRMADO por diff: Brain cambió únicamente `tests/browser_auth.cjs` y `.github/workflows/clean-snapshot.yml` en ese incremento. Agregó paths y pasos HTTP/Chromium, mantuvo contents:read y checkout sin credenciales persistidas, y amplió el job de 5 a 10 minutos, no los timeouts previos. Mi afirmación histórica de que CI excluía los tests era válida para el head anterior; ya NO describe el actual.

## 2. PR19: referencia versionada

Reejecución del banco del autor, desde archive nuevo del head: exit0 y 37 checkpoints aprobados en Chromium153.0.8010.12, Playwright1.63.0. No se cuentan como 37 pruebas independientes del auditor ni se repitieron aquí los 118 Python históricos.

Instrumento adicional: `tests/audit_browser.cjs`, fuente completa incluida en evidencia. Reutiliza arranque/limpieza y once controles positivos del autor; agrega ocho checkpoints propios con expectativas separadas del exportador. Resultado: 19 checkpoints totales pasan y vuelven a pasar en copia/fixture nuevos. No se suman las repeticiones como casos nuevos.

Controles propios e impacto:

- Descarga positiva real, JSON analizado, 16 campos y versión fija, sin bearer.
- Tras lectura completa, deshabilitar colección: no descarga, borra texto/referencia pendiente y deshabilita Guardar.
- Lo mismo al retirar membresía, deshabilitar usuario o retirar documento.
- Vencer la sesión persistente antes de guardar: mismo rechazo. SQL cambia ambas fechas a un intervalo válido ya vencido, no rompe el esquema.
- Respuesta503 al guardar: no descarga ni conserva referencia pendiente.
- Inyectar campos adicionales `extra_private_note`, `access_token` y `password` en las respuestas: ninguno se exporta. Tokens sintéticos usados en memoria, jamás capturados en logs.

Falsador: reemplazar exclusivamente `const checked = metadata(await readPage(0, 1));` por `const checked = selected;` en otra copia. El positivo de descarga sigue pasando; falla `independent_denied_collection`, capturado pass:false y AssertionError pertinente. Esto prueba sensibilidad a omitir la revalidación, no una caída genérica del proceso. Ningún mutante se publica como producto.

No se encontró bypass en esos intentos. No demuestra ausencia universal de fallos. Las denegaciones ejercitan política real por HTTP; el503 y los campos adicionales son respuestas intervenidas del consumidor, expresamente separadas de la política.

## 3. Diagnóstico de piloto, informe13

CONFIRMADO por fuentes: la demo valida fixture y usuarios ficticios; PR19 no cambia backend/autenticación. El handler legacy de main entrega `/api/v1/buscar` y documentos sin atravesar autorización v2, y `/api/v1/revision` lee una cola, no recibe reportes. Restore declara cuarentena y `serve_authorized:false`, sin reapertura automática. No se probó exposición remota actual de esas rutas.

La enmienda v2 exige muestra jurídica/privacidad M01, snapshot M02, seguridad/recuperación M03 y tareas observadas M04. Los pagos posteriores a G08 no son prerrequisito del piloto gratuito. El diagnóstico distingue correctamente demo, piloto y producción; no convierte las 169 omisiones de fixtures en bloqueo universal ni las 323h históricas en saldo/ETA.

NO MEDIDO en esta auditoría: existencia actual de acuerdos humanos fuera de fuentes accesibles. No repetí todas las búsquedas de Brain; no adopto «no acreditado en aquella búsqueda» como prueba de ausencia de responsables, consentimiento o actas. Tampoco acepto el diagnóstico como autorización para integrar, elegir stack o desplegar.

## 4. Custodia, CI y proceso

Evidencia de Brain12 recuperada desde git: decodificación estricta85779bytes, SHA256 `e0b8316179fdd67e605e3cd47c10914b5f3f8de8cabd38c5ae1d877302cbae81`, coincide con su recibo. Leídos informes11/12/13, diff real y fuentes del consumidor/política/lector. El informe11 se leyó en sus secciones sustantivas; su bloque crudo histórico no se revalidó en esta unidad. No se verificó de nuevo la cifra169.

CI final de PR19 job105990917105: completed/success; los nueve pasos incluyen la frontera HTTP y Chromium. Se recuperaron atributos completos del job, no stdout remoto: los37checkpoints citados son de la reejecución local. URL: https://github.com/gatehot59-star/corpus-legal-tarija/actions/runs/35478146323/job/105990917105

La clasificación LIGERO del cambio de workflow11 fue incorrecta y Brain ya lo reconoce en12; el verde no borra ese error metodológico. No se inventa autorización retrospectiva ni se otorga nota para compensarlo. No se reenvió solicitud a Copilot: esta auditoría no depende de que ese pedido haya sido aceptado.

Entorno medido: brain-env, navegador/runtime privados ya presentes; inventario canónico leído como mapa, no permiso. No se usaron GPU ni nueva VM, no se instalaron dependencias globales. Los procesos de servidor informan exit0; contextos/navegadores se cierran en finally. Escaneo /proc posterior no encontró CLI/runners de las rutas propias. No es inventario total de todos los procesos del host.

Error propio: una consulta preparatoria con backticks fue rechazada por el shell antes de ejecutar Python. Repetida sin backticks; no se contó como fallo de producto y no hubo corrida experimental descartada.

Evidencia propia: `docs/agents/evidencia/2026-09-19-14-astra-brain-pr18-pr19.json`, xz+base64,29465bytes decodificados, SHA256 `4b7d0d05dfa4637c61344aa18cbcd0e477b331d4ac322bd15b4eb62269c9c871`. Preserva fuente completa del instrumento, comandos/directorios/exits/stdout/stderr de todas las corridas, mutación exacta, manifest de fuentes, refs, atributos CI y escaneo. Ningún bearer, contraseña real, DB o binario se publica. Decodificar payload con base64.b64decode(validate=True) y lzma.decompress; cotejar bytes y sha256.

## 5. Qué pedirle a Brain ahora (recomendación, no orden enviada)

Primero dejar explícito que PR19 depende de PR18: no integrar PR19 contra main como si fuera independiente. Si se decide mergear, obtener autorización específica y volver a comprobar heads/CI; esta auditoría no la concede.

El siguiente trabajo útil es una ficha de piloto y contrato de integración: tarea jurídica concreta, muestra UID/versión/fuente, responsable de revisión/edición, participantes y datos permitidos, autorización y tope de recursos. Lo desconocido queda PENDIENTE, sin inventar personas ni actas. En paralelo se puede proponer el corte mínimo de cuentas/búsqueda/feedback protegidos y recuperación, sin relajar el lanzador ficticio ni reutilizar v1 como atajo. Propuesta y decisión antes de implementación/despliegue.

No dedicar la siguiente unidad a otra ronda general de cierres SQLite o a subir un score. Reparar deuda cuando una aceptación concreta la necesite. No interpretar esta recomendación como aprobación de arquitectura nueva, mensajes, asignaciones o contacto institucional.

## Gates y exclusiones

Gate I PASA para las propiedades declaradas: baseline, réplica en copia nueva, falsador causal, fuente/captura y cierre propios. Controles heredados y propios se distinguen. La independencia es del esperado, no del nombre Astra.

Gate II PASA acotado: afirmaciones verificadas y límites separados; no fallo nuevo de producto demostrado, sí confirmación del antiguo punto ciego Unicode. Sin nota de producto ni firma de merge.

Gate III: publicación de estos dos archivos, recuperación/decodificación y comparación remota, Doc público en Space y Nexus se verifican antes de responder; el identificador del cierre queda en esos registros. Git es el informe canónico y el Doc su espejo legible.

Qué no se midió que importaba: auditoría completa de autenticación/Origin, carreras/BFCache, todos los tipos de metadatos, cadena CVE, visual/accesibilidad, otros navegadores, carga, producción/TLS/VM, revisión jurídica, acuerdos y utilidad de piloto. No se redistribuyó credencial, se tocó producto ni se mergeó/desplegó nada.

--- METODO TITAN ---
Modo: auditoría acotada con pruebas adversas, no implementación. Máquina: brain-env. Instrumentos y evidencia arriba. N/A: producción, innovación de funciones, despliegue y nueva ejecución de toda la suite Python. PR18/19 abiertos, sin aprobación de merge. Cierre: git+Doc público+Nexus, no aprobación por score.
