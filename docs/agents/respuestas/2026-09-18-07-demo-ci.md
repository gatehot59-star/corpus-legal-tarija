# PR15: el lanzador de la demo ya se ejecuta en CI, sin merge ni despliegue

18-sep-2026 ART. Pedido:continuar con Astra y el plan;aprobacion explicita ClickKit approve_corpus_demo_ci para modificar workflow,abrir PR de seguimiento y ejecutar GitHub CI,sin merge/despliegue. Cambio de rol declarado:implementador/verificador de CI,no auditor independiente de codigo que implemento Brain.

## Resultado

[PR15](https://github.com/gatehot59-star/corpus-legal-tarija/pull/15),titan/isolated-demo-ci sobre titan/isolated-demo-launcher. Commit84cae9a793715355a29fd692fe097003349aa316,base5ac0da781c53de9ffbe2a0752e33ab7aa87b33d2(PR14). Un archivo cambiado: .github/workflows/clean-snapshot.yml,9lineas agregadas. No cambios de producto ni sobreescritura de la rama PR14.

[Job GitHub](https://github.com/gatehot59-star/corpus-legal-tarija/actions/runs/35412033482/job/105813396218) stable_identity completed/success. Inicio2026-09-19T01:14:40Z,fin01:15:12Z (18-sep22:14:40..22:15:12ART). No se infiere ejecucion de demo solo del verde general:el HTML publico tiene data-name Compile and test synthetic demo CLI (real subprocess lifecycle),numero4,data-conclusion success,inicio01:14:58Z,fin01:15:09Z. El step de regresion previo tambien success. Estos atributos completos estan en el JSON de CI.

PR14 yPR15 quedan abiertos/sin merge. Esta corrida valida el snapshot del lanzador de PR14 con el workflow nuevo de PR15;NO agrega retrospectivamente un check al SHA viejo de PR14. El filtro nuevo queda propuesto en rama,hasta integrar el workflow. No se afirma que ya este activo en main.

## Contexto recuperado y prioridad

Se leyeron plan/enmienda v2,revision Astra09,recibo demo06,Nexus46/47,codigo y banco PR14,workflow y reviews/checks. Astra no encontro un defecto nuevo de persistencia;confirmo que el Handler historico no componia sesiones. Otro turno ya implemento el lanzador aislado en PR14:se reutiliza,no se duplica. El hueco posterior era medible:check_runs0y paths ausentes. Se pidio permiso para ese cambio de workflow/runtime antes de hacerlo.

Esto es soporte tecnico a la composicion aislada y verificabilidad previa a demo,NO cierre de M03/M04,D09,piloto o gates humanos. El plan sigue exigiendo restauracion,seguridad real,revision juridica y aprobaciones de muestra. No se importan pendientes de MUDH/Android.

## Cambio quirurgico

Agregados al filtro PR:sistema/api/demo_aislada.py,tests/test_demo_aislada.py,sistema/DEMO-AISLADA.md. Step separado con bash,set-euo-pipefail,py_compile de modulo+banco y timeout120s python3 tests/test_demo_aislada.py. Sin if ni continue-on-error.

Los pasos anteriores son identicos estructuralmente a la base. Se conservan checkout SHA11d5960a326750d5838078e36cf38b85af677262,persist-credentials:false,contents:read,ubuntu24.04,tope5min y todos los timeouts anteriores. Sin installs,secretos,write permissions,upload externo,despliegue ni workflow manual disparado. GitHub corrio automaticamente por abrir el PR autorizado. No se altero el servidor ni se expuso una demo permanente.

Git diff confirma1archivo9insertions. Workflow remoto recuperado desde84cae9a y comparado byte por byte con el ensayado:True. No se vuelve a adjudicar el92.86%coverage del turno anterior:aca no se midio cobertura.

## Pruebas locales propias y controles

Python3.12.14,SQLite3.46.1,PyYAML6.0.3 en brain-env via build/run. BaseLoader evita tratar on como bool de YAML1.1. Instrumento docs/agents/evidencia/2026-09-18-07-demo-ci-verifier.py.txt:fuente exacta ejecutada;obtiene y corre los bloques run del YAML,comprueba estructura y conserva stdout/stderr completos. PyYAML se usa solo en el verificador,no se agrega al producto ni al workflow.

Dos bloques positivos exit0. Regresion:21SOL5.966s+15HTTP1.480s+15access5.190s+18guards23.013s+10session14.082s=79. Demo18tests32.508s. Total97tests locales,no se cuentan los12login heredados dos veces. El banco demo levanta procesosCLI reales con datos ficticios yHTTPloopback:pagina exacta,logoutselectivo,rearranque de proceso,guards filesystem/Host/Origin y cleanup. No es produccion.

Falsador local en copia separada:solo if not args.isolated_demo: reemplazado por if False:. Se ejecuta el mismo bloque shell nuevo completo. Exit1,AssertionError0!=2 en test_init_requires_opt_in_without_writes. El mismo mutante tambien deja arrancar serve sin opt-in y test_serve_requires_opt_in_without_mutation produce TimeoutExpired a15s:se conserva el error completo y no se oculta ni se presenta como fallo distinto del producto. Resultado18tests48.852s,failures1/errors1. El rojo no depende unicamente del timeout:la assertion de init es explicita. No se ejecuto una corrida roja intencional en GitHub.

El escaneo final de procesos Python propios del scratch no encontro demo_aislada.py activa. Los positivos verifican apagado y puerto cerrado;subprocess.run termina/recolecta el hijo que excede timeout en el mutante. No se certifica ausencia de servicios ajenos ni crash recovery.

## Evidencia del runner y limites de captura

MCP check_runs entrega structured completed/success y tiempos. La API anonima de jobs devolvio403rate limit:no se declaro fallo del CI ni se buscaron tokens. Se leyo la pagina publica del job y se extrajeron todos los atributos de sus seis elementos de pasos,con nombres,success,tiempos y URL vinculada al commit84cae9a. El endpoint de logs del paso devolvio404:stdout/stderr COMPLETOS del runner NO recuperados. No se confunden logs locales con GitHub ni se copia nuestra prosa del commit como evidencia de ejecucion.

La cuenta97esta sustentada por stdout/stderr locales y el workflow leido. GitHub confirma que ambos pasos terminaron correctamente;no se afirma haber contado97individuales en un log remoto que no se obtuvo. El HTML completo214707bytes queda local con SHAb13e8560043b8057a8b6bfee5a32ea9c83d228e58f9e839733d229177850ca4d;Git recibe la captura explicita de atributos,no se la denomina HTML completo.

GitHub muestra una advertencia heredada:checkout pinneado apunta a Node20 y el runner lo fuerza a Node24. El job paso;no se cambia version por impulso ni se declara CVE. Actualizacion de checkout queda como seguimiento separado si corresponde.

Capturas legibles en docs/agents/evidencia/:2026-09-18-07-demo-ci-local.json(los3procesos enteros),2026-09-18-07-demo-ci-runner.json(atributos completos seleccionados y limites),2026-09-18-07-demo-ci-verifier.py.txt(instrumento). Sin credenciales reales,Authorization ni cuerposHTTP guardados;el banco no imprime los tokens ficticios. Se recuperan de Git y comparan con capturas antes del chat. Informe/documentacion a main bajo permiso documental;workflow permanece solo en PR15.

## Errores de preparacion registrados

El sandbox auxiliar no tenia yaml:la comprobacion fallo antes de ejecutar;se compilo alli el script sin importar dependencias y se parseo YAML en brain-env,donde6.0.3fue medido. La transferencia por attachment se verifico SHA32c07c1722a3fd35dabc57f57303f3a14facdc2f297fcdbd89dcccb410950d55. Dos primeras consultas de resultados llegaron antes de terminar el falsador:una assert de completitud fallo por captura aun de2filas,se espero la misma corrida y finalizo con3,sin relanzar duplicados. No defectos de producto atribuidos a eso.

## Gate y QA

Afirmacion->instrumento->podia dar rojo->resultado/limite:
1.Workflow incluye y ejecuta demo->parse YAML+runverbatim+GitHub step4->si->CONFIRMADO en PR15.
2.Regresion conservada->comparacion estructural pasos previos+79tests->si->CONFIRMADO en alcance probado.
3.Gate propaga fallo real->mismo bloque con mutante->si->exit1 con assertion objetivo;rojo LOCAL,noGitHub.
4.Producto listo para piloto->ninguno->no->NO MEDIDO.

GateI PASA acotado a integracion de CI:versionexacta,positivo,falsador,captura y limpieza;oraculos del banco son del autor,NO nueva auditoria independiente. GateII PASA si se mantienen separados local/runner,propuesto/integrado y prueba/produccion. GateIII se acredita tras readback y Docpublico/Nexus;no se inventa completitud anticipada dentro de estos bytes. N/A:producto nuevo,legal,despliegue,revisiones humanas. No puntaje nuevo ni autorizacion de merge por contar tests.

Siguiente dependencia:revision de los PR14/15 e integracion con autorizacion separada;no se gasta el permiso anterior de7PRotra vez. Luego recorrido de usuario y restauracion/seguridad acotados al plan,sin dar por aprobados gates humanos. PR1 sigue fuera de este incremento.
