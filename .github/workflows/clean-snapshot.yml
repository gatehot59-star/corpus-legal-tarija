name: Clean snapshot identity
on:
  pull_request:
    paths:
      - 'pipeline/clean_snapshot.py'
      - 'pipeline/legal_html.py'
      - 'sistema/api/ingesta.py'
      - 'sistema/api/esquema.sql'
      - 'tests/test_clean_snapshot.py'
      - '.github/workflows/clean-snapshot.yml'
  workflow_dispatch:
permissions:
  contents: read
jobs:
  stable_identity:
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          persist-credentials: false
      - name: Compile and test full-copy integration
        shell: bash
        run: |
          set -euo pipefail
          python3 -m py_compile pipeline/clean_snapshot.py tests/test_clean_snapshot.py
          python3 tests/test_clean_snapshot.py
